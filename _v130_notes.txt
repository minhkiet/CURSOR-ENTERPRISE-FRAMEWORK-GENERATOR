v1.3.0 — fix: re-export enums + fix count_code_lines

Re-export public enums from cursor_framework/__init__.py so tests and external
importers can do `from cursor_framework import X`:

- IntentType, Domain, Skill (from context_router)
- CompressionStrategy, CompressionResult (from token_optimizer)

Fix count_code_lines() in cursor_framework/utils/code_utils.py:

- Use splitlines() instead of split('\n') so trailing newline does not
  inflate total line count
- Subtract comments from the 'code' field (was only subtracting blanks),
  so 'code' now means non-blank + non-comment lines

Tests: pytest 77 passed (cursor_framework/tests) + 59 passed (tests/) =
136 total, 0 failures.